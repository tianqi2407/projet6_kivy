#:kivy 1.8.0
#:import datetime datetime

RootWidget:
    # import container
    container: container

    # fill the container
    BoxLayout:
        id: container
        orientation: 'vertical'
        padding: 0
        spacing: 3

        Label:
            text: 'choose a scenary'
			font_size: '30sp'
        BoxLayout:
            orientation: 'horizontal'
            padding: 0
            spacing: 1
            size_hint: 1, 0.1

            # weiter button
			Button:
				text: 'add'
			Button:
				text: 'Previous screen'
            Button:
                text: 'Next screen'
                on_release: app.next_screen('wall')
